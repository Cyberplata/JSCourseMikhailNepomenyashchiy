import {sum, addOne} from './first';    // Могли бы использовать несколько, если бы потребовалось
import {firstName} from './second';

console.log('Hello from entry point', firstName);
console.log(sum(2, 4));